{
  "name": "reading-tracker-api",
  "version": "1.0.0",
  "description": "Azure Functions backend for Reading Tracker",
  "main": "index.js",
  "scripts": {
    "build": "npm run clean && npm run copy",
    "clean": "rm -rf dist",
    "copy": "mkdir -p dist && cp -r . dist/ && cd dist && zip -r function.zip . -x '*.git*' 'node_modules/.cache/*' 'dist/*'",
    "start": "func start",
    "test": "echo \"No tests specified\""
  },
  "dependencies": {
    "@azure/functions": "^4.0.0",
    "openai": "^4.20.0"
  },
  "devDependencies": {
    "azure-functions-core-tools": "^4.0.0"
  }
}
